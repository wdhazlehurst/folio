//import { Prisma } from "@prisma/client";

// Controls user only content
//type BaseOpts = { userId: string };

//type ExpensesOnly = BaseOpts & {
    //mode: "expenses";
    //limit?: number;
//};

//type CategoryOnly = BaseOpts & {
    //mode: "categories"
//}